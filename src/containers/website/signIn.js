import PropTypes from "prop-types";
import Signin from "../../components/website/home/Signin";
const SignIn = (props) => {
  return (
    <>
      <Signin/>
    
     
    </>
  );
};

SignIn.propTypes = {
  default: PropTypes.object,
};

export default SignIn;
